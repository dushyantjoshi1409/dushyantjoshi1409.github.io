# Required — Claude API for LLM generation
ANTHROPIC_API_KEY=sk-ant-...

# Optional — Langfuse tracing (graceful fallback to file logging if not set)
LANGFUSE_PUBLIC_KEY=pk-...
LANGFUSE_SECRET_KEY=sk-...
LANGFUSE_HOST=https://cloud.langfuse.com
